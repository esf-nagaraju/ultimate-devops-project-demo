name: Matrix CI
on: [ push ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        java: [11, 17]
    steps:
      - run: echo "Running on ${{ matrix.os }} and Java Version ${{ matrix.java }}"
