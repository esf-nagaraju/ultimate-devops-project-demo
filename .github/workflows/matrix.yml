name: Matrix CI
on: [ push ]


jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        java: [11,17]
    steps:
      - uses: actions/checkout@v3

      - uses: .github/actions/setup-java
        with:
          java: 17
    
